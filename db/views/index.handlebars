<h1 id="head">Lukes Premium <a id="apples">cardiac health </a> & burger joint clinic</h1>

<h2 id="welcome">Welcome to the burger tracker, you dont watch your caloric intake but you do watch those tasty cow circles<br>
Here's the ones you havent eaten yet</h2>

<ul class="list">
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2 id="devoured">burgers that are devoured!</h2>
<i id="burgerRight" class="fas fa-hamburger"></i>
<i id="burgerLeft" class="fas fa-hamburger"></i>
<ul class="list">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2 id="add">Add a burger</h2>
<form id="addform" class="create-form">

  <div id="newBurgerInput" class="form-group">
    <label for="ca">burger Name:</label>
    <input type="text" id="ca" name="name" placeholder="burger name">
  </div>

  <div class="form-group">
    <label id="devy" for="devoured">Devoured it?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Not eaten yet
  </div>

  <button type="submit">Add burger</button>
</form>
