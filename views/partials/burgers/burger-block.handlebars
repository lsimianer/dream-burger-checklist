<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}} eat this for america!!!{{else}}oof,I ate that!!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">No Thanks, delete this one!</button>
</li>
